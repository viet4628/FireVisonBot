# Trọng số YOLO (máy tính / laptop)

Đặt file **`yolo_best.pt`** (hoặc model `.pt` bạn train) vào thư mục này.

- **Dashboard** (`dashboard/main.py`) tự tìm: `models/yolo_best.pt`
- **Script OpenCV** (`scripts/yolo_mjpeg_viewer.py`) dùng cùng đường dẫn

File `.pt` thường rất nặng; có thể không commit lên git — mỗi máy copy vào đây sau khi clone.
